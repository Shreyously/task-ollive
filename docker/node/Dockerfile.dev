FROM node:20-alpine

RUN apk add --no-cache curl openssl libc6-compat

WORKDIR /workspace

# Keep install step explicit; source is bind-mounted in compose for hot reload.
CMD ["sh", "-lc", "npm install && npm run dev"]

